@model IEnumerable<SGHR.Application.Dtos.Configuration.Reservas.ServicioAdicional.ServicioAdicionalDto>

<style>
    /* Columna de acciones compacta */
    th.acciones, td.acciones {
        width: 120px; /* ancho fijo pero suficiente para 2 botones */
        white-space: nowrap; /* evita que los botones se separen en varias líneas */
    }

    /* Ajuste de botones para no separarse demasiado */
    .btn-accion {
        margin: 0 2px; /* espacio pequeño entre botones */
    }

    /* Cursor pointer en filas */
    .fila-usuario {
        cursor: pointer;
    }
</style>

<table class="table table-striped table-bordered table-hover">
    <thead class="table-dark">
        <tr>
            <th>ID</th>
            <th>Nombre</th>
            <th>Precio</th>
            <th>Estado</th>
            <th class="acciones text-center">Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var servicio in Model)
        {
            <tr data-id="@servicio.Id" class="fila-servicio">
                <td>@servicio.Id</td>
                <td>@servicio.Nombre</td>
                <td>@servicio.Precio</td>
                <td>@servicio.Estado</td>
                <td class="acciones text-center">
                    <a class="btn btn-sm btn-primary btn-editar btn-accion"
                       asp-action="Edit"
                       asp-controller="ServicioAdicional"
                       asp-route-id="@servicio.Id">
                        Editar
                    </a>
                    <button class="btn btn-sm btn-danger btn-eliminar btn-accion" data-id="@servicio.Id">
                        Eliminar
                    </button>
                </td>
            </tr>
        }
    </tbody>
</table>
